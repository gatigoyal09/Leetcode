class Solution {
    public int gcdOfOddEvenSums(int n) {
        // int so = 0;
        // int se = 0;
        // for(int i=0; i<=n; i += 2){
        //     se += i;
        // }
        // for(int i=1; i<=n; i += 2){
        //     so += i;
        // }
        // while(so!=0){
        //     int temp = so;
        //     so = se % so;
        //     se = temp;
        // }
        // return Math.abs(se);
        return n;
    }
}